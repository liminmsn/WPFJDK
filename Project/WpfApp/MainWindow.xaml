<Window 
	x:Class="WpfApp.MainWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
	xmlns:local="clr-namespace:WpfApp"
	mc:Ignorable="d"
	ResizeMode="NoResize"
	WindowStartupLocation="CenterScreen"
	WindowStyle="None"
	Background="Transparent"
	AllowsTransparency="True"
	Foreground="{Binding Color_Text_0}"
	Height="450"
	Width="800">
	<Window.Resources>
		
	</Window.Resources>
	
	<Border x:Name="main_box" Background="{Binding Color_0}" CornerRadius="6" Padding="4">
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="30"/>
				<RowDefinition/>
			</Grid.RowDefinitions>
			<Border Grid.Row="0"
			MouseLeftButtonDown="Border_MouseLeftButtonDown" 
			CornerRadius="{Binding ElementName=main_box,Path=CornerRadius}"
			Background="Transparent" Cursor="Hand" Padding="4,0,0,0">
				<DockPanel Cursor="Hand" VerticalAlignment="Center">
					<TextBlock VerticalAlignment="Center" Text="{Binding Title}" FontWeight="Bold"/>
					<Button HorizontalAlignment="Right" Padding="4,3,4,3" FontSize="20" FontFamily="Arial Black" Margin="0,2,0,2">
						<ui:SymbolIcon Symbol="DismissCircle12" Foreground="Red" MouseDown="SymbolIcon_MouseDown" />
					</Button>
				</DockPanel>
			</Border>
			<DockPanel Grid.Row="1">
				<Border
				Background="{Binding Color_1}"
				CornerRadius="{Binding ElementName=main_box, Path=CornerRadius}">
					<DockPanel HorizontalAlignment="Stretch">
						<ListView ItemsSource="{Binding PageManages}" Background="Transparent" BorderThickness="0" d:ItemsSource="{d:SampleData ItemCount=5}">
							<ListView.ItemContainerStyle>
								<Style TargetType="ListBoxItem">
									<Setter Property="HorizontalContentAlignment" Value="Stretch"/>
									<Setter Property="Padding" Value="0"/>
									<Setter Property="Margin" Value="0"/>
								</Style>
							</ListView.ItemContainerStyle>
							<ListView.ItemTemplate>
								<DataTemplate>
									<Button Tag="{Binding Page}" Click="Button_Click" HorizontalAlignment="Stretch">
										<Button.Content>
											<DockPanel>
												<ui:SymbolIcon Symbol="{Binding Icon}"/>
												<TextBlock Margin="4,0,0,0" Text="{Binding Label_}"/>
											</DockPanel>
										</Button.Content>
									</Button>
								</DataTemplate>
							</ListView.ItemTemplate>
						</ListView>
						<Separator/>
						<Border Padding="2">
							<Frame x:Name="frame" />
						</Border>
					</DockPanel>
				</Border>
			</DockPanel>
		</Grid>
	</Border>
</Window>
